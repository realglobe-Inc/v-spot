/**
 * Test for VSpotClient.
 * Runs with mocha.
 */
'use strict'

const VSpotClient = require('../lib/VSpotClient')
const { ok, equal } = require('assert')

describe('v-spot-client', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
