/**
 * Test for VSpotServer.
 * Runs with mocha.
 */
'use strict'

const VSpotServer = require('../lib/VSpotServer')
const { ok, equal } = require('assert')

describe('v-spot-server', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
